specVersion: "v1beta"
serviceId: "listado-personas"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "listado-personas-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
